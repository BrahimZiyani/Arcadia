# config/services.yaml

parameters:
    uploads_directory: '%kernel.project_dir%/public/uploads'

services:
    # Default configuration for services in this file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            # Default binding for the uploads directory
            $uploadsDirectory: '%uploads_directory%'

    # Makes classes in src/ available to be used as services
    # This creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
    
    # Explicit service definition for HabitatService
    App\Service\HabitatService:
        arguments:
            $uploadsDirectory: '%uploads_directory%'

    # Add other specific service definitions if needed
    App\EventListener\CompteRenduListener:
        tags:
            - { name: doctrine.event_listener, event: postPersist }
